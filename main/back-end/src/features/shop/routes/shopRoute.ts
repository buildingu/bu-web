/**
 * U-Shop HTTP Routes
 */

import { Router } from "express";
import * as shopController from "@shopFeat/controllers/shopController";

const router = Router();

// router.get("/items", shopController.getItems);

// router.post("/payment", shopController.processPayment);

export default router;
