/**
 * Partner HTTP Routes
 * 
 * Includes:
 * - Connect with us.
 * - Partner with us.
 * - Contribute (includes donations).
 */

import { Router } from "express";
import * as partnerController from "@partnerFeat/controllers/partnerController";

const router = Router();

export default router;