// TODO:
// Could add other user things here too, like profile credentials, the user that you use in decodedClaims maybe, etc.

export default interface UserCredentials {
  // The entire user saved in the db.
}
