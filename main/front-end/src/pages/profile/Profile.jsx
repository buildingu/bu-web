import { Layout } from "../../components/layout";
import s from "./styles.module.css";

const Profile = () => {
  return <Layout className={s.container}><main>Profile</main></Layout>;
};

export default Profile;
